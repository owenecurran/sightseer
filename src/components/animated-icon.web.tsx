// Web has no native splash screen to hand off from (expo-splash-screen is
// native-only), so there's nothing to animate away here — the app just
// mounts directly.
export function AnimatedSplashOverlay() {
  return null;
}
